<div class="jumbo">
  <div class="right tomster"></div>
  <h2>Welcome!</h2>
  <p>We hope you find exactly what you're looking for in a place to stay.</p>
  {{#link-to 'about' class="button"}}
    About Us
  {{/link-to}}
</div>


{{!-- {{#each model as |rental|}}
    <h3>{{rental.name}}</h3>
{{/each}} --}}

{{#each model as |nameUnit index|}}
  {{radio-button checked=nameUnit.checked value=index othersSholdBeFalse='falseOthers'}}{{nameUnit.name}}
{{/each}}

<button class="btn btn-danger" {{action 'del' model}}>删除</button>

<br/>
<br/>
<br/>




{{!-- 
{{#each model as |mm|}}
  <h2 {{action 'toggleColor'}} class="{{if isYellow "nameYellow"}}"> {{mm.name}}</h2>
{{/each}}
 --}}